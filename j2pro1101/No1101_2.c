#include <stdio.h>
#include <stdlib.h>

int main(){
	int m,n,b;
	int i,j,k;
	int *map;

	printf("input m: ");
	scanf("%d",&m);
	printf("input n: ");
	scanf("%d",&n);
	printf("input bomb amount: ");
	scanf("%d",&b);

	map = (char *)malloc(sizeof(int)*m*n);

	for(i = 0; i <  ; i++){
	for(i = 0; i < (m*n) ; i++){
		password[m][n] = rand();
	}
	password[i] = '\n';


	free(password);
	return 0;
}
